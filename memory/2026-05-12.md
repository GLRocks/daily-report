

## 2026-05-12 18:00 - 格式锁定生效

### 用户最终指令
> "以这个推送为今后的模板，保证每日更新不要随意变更格式，请你将系统配置按这个确保更新成功，你失败太多次了"

### 完成的锁定操作
1. **HTML模板固化**：`daily_report_2026-05-12.html` + `agentic_market_daily_template_v12.html` 已修复嵌套问题，66,687字节，12板块，投资人论点第2位
2. **Cron配置更新**：`/root/.openclaw/cron/jobs.json` payload 中加入「V12模板强制要求（格式锁定，不可变更）」条款
3. **FORMAT_LOCK.md 创建**：记录不可变更项（标题、主题色、板块顺序、股票面板格式、CSS结构）
4. **HTTP server + Cloudflare Tunnel**：`https://nature-remainder-sys-from.trycloudflare.com/daily_report_2026-05-12.html`

### 验证结果（URL可访问）
- 标题：Agentic Market Daily ✓
- 蓝色主题：#38bdf8 ✓
- 红色已清除：#e94560 不在 ✓
- 12板块顺序正确 ✓
- 21只股票面板 + BUY/HOLD/SPEC BUY标签 ✓

### 关键修复：因果链嵌套问题
- Transform 脚本 bug 导致因果链 section header 被嵌套在开源社区 section 的 content div 中
- 已用 `/tmp/fix_nested.py` 移除嵌套的 `<h2>因果链分析 & 投资预测</h2>` 和 `<span class="badge badge-analysis">深度分析</span>`
- 保留因果链的具体内容（传导路径、投资预测、推荐标的）在 section 8 内
- 修复后 h2 数量从 13 → 12，与板块数量一致

### 教训
1. **格式锁定比内容更重要**：用户能接受内容质量波动，但格式变更会直接触发信任危机
2. **模板文件是唯一的真值来源**：`agentic_market_daily_template_v12.html` 是每日生成的基准，任何偏离都是 bug
3. **cron payload 必须包含格式锁定条款**：否则模型可能在不同 session 中「创新」格式
4. **FORMAT_LOCK.md 是防御性文档**：当未来的 session 试图变更格式时，可以被引用作为不可变更的证据
5. **Cloudflare Tunnel URL 是临时的**：每次启动都会生成新 URL，但用户主要依赖推送的消息中的链接，而非收藏固定URL

### 下一次运行验证
- 2026-05-13 8:07 cron 将测试新的格式锁定配置
- 关键验证点：模板结构是否零变更、内容是否新鲜、URL 是否正常发布

## 2026-05-12 18:45 - 用户最终确认与记忆强化

### 用户指令
> "请认真记住上述模板及格式，以后我再微调 不要把模板其他格式忘记了"

### 关键记忆锚点
1. **模板是神圣不可侵犯的**：用户说"微调"指的是内容微调，不是格式微调
2. **格式记忆必须持久化**：写入 MEMORY.md + FORMAT_LOCK.md + cron payload 三重保险
3. **未来任何session接到"调整"指令时**：先读 FORMAT_LOCK.md，确认是内容调整还是格式调整；如果是格式调整，必须向用户确认"您要调整的是内容还是模板结构？"
4. **防御性编程**：即使未来session丢失上下文，cron payload中的强制要求 + FORMAT_LOCK.md 文件足以恢复正确格式

### 模板真值源（三保险）
| 保险层级 | 文件路径 | 用途 |
|---------|---------|------|
| 第一层 | `/root/.openclaw/workspace/agentic_market_daily_template_v12.html` | HTML结构真值源，每日复制此文件作为起点 |
| 第二层 | `/root/.openclaw/workspace/FORMAT_LOCK.md` | 人工可读的格式锁定文档 |
| 第三层 | `/root/.openclaw/cron/jobs.json` payload | 每日cron指令中的强制要求条款 |

### 已锁定内容（不可遗忘）
- **13板块顺序**：1股票→2投资人→3AI独角兽→4芯片→5中国云→6AI Agent→7Agent标准化→8开源社区+因果链→9ToC硬件→10全球交易→11政治→12Gen Z→13个性化推荐
- **CSS变量**：`--accent: #00d4ff`, `--highlight: #e94560`, `--bg: #0a0e1a`, `--card: #0d1f35`
- **特殊样式类**：quote-box/play-btn, insight-box, signal-list/tag-hot/tag-new, causal-chain, highlight-stock
- **股票面板**：21只，rec-badge(BUY/HOLD/SPEC BUY) + cat-badge(芯片/应用/能源) + stock-metrics + stock-reason
- **板块标题格式**：`<div class="section-title"><span class="num">{n}</span> {名称}</div>`
- **表格样式**：data-table，表头 #0a1628 + var(--accent)，斑马纹 var(--card2)
